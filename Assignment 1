#include<stdio.h>
#include<conio.h>
main()
{
    int min,max,c,n,i,count=0;
    scanf("%d%d",&min,&max);
    for(n=min;n<=max;n++)
    {
        i=1;c=0;
        while(i<=n)
        {
            if(n%i==0)
            {
            c++;
            }
            i++;
        }
        if(c==2)
        count++;
    }
    printf("%d",count);
}
